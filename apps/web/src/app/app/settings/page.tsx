export default function SettingsPage() {
  return (
    <div>
      <h1 className="display-lg">Settings</h1>
      <p className="caption" style={{ marginTop: 'var(--space-3)' }}>
        Profile + preferences surface lands here (task 11).
      </p>
    </div>
  );
}
